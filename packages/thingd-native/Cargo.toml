[package]
name = "thingd-native"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Native Node.js binding for thingd."
keywords = ["ai", "memory", "napi", "nodejs", "sqlite"]
categories = ["api-bindings", "database-implementations"]

[lib]
crate-type = ["cdylib"]
path = "native/src/lib.rs"

[dependencies]
thingd-core = { path = "../../crates/thingd-core", features = ["sqlite"] }
napi = { version = "3.3.0", default-features = false, features = ["napi4"] }
napi-derive = "3.3.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"

[build-dependencies]
napi-build = "2.2.4"
