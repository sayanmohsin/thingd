pre-push:
  parallel: true
  commands:
    lint:
      run: pnpm check
    build:
      run: pnpm build
    rust-fmt:
      run: cargo fmt --all --check
    rust-clippy:
      run: cargo clippy --workspace --all-targets --all-features -- -D warnings
