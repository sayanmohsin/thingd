[package]
name = "thingdb"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Experimental Rust-native durable ordered key-value engine for Thingd."
readme = "README.md"
keywords = ["database", "key-value", "storage", "rust", "thingd"]
categories = ["database-implementations"]

[dependencies]
crc32fast = "1.4"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[dev-dependencies]
tempfile = "3.27.0"

[lints]
workspace = true
