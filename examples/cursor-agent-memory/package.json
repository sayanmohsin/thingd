{
  "name": "thingd-cursor-agent-memory-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "pnpm build && node dist/quickstart.js",
    "scheduler": "pnpm build && node dist/scheduler-heartbeat.js"
  },
  "dependencies": {
    "@thingd/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "@types/node": "26.3.0"
  }
}
