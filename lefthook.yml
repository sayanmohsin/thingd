pre-push:
  parallel: true
  commands:
    lint:
      run: pnpm check
    build:
      run: pnpm build
    test-node:
      run: pnpm test:node
    rust-fmt:
      run: cargo fmt --all --check
    rust-clippy:
      run: cargo clippy --workspace --all-targets --all-features -- -D warnings
