[package]
name = "memoryd-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core primitives for memoryd, an object-shaped local memory engine for apps and agents."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
